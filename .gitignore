# Dependencies
node_modules/
vendor/
.pnp/
.pnp.js

# Build output
dist/
build/
out/
.next/

# Environment
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
env/
*.sqlite
*.sqlite3
*.db
*.db-journal

# Dev preview scripts (local only — not for production)
apps/backend/_dev_*.py
apps/backend/dev.db

# Local Postgres data (when running compose with a bind mount)
pgdata/
postgres-data/

# Local Caddy data (when running prod compose locally)
infra/caddy_data/
infra/caddy_config/

# Backup / temp files
*.bak
*.tmp
*.orig

# IDE
.vscode/
.idea/
*.swp
*.swo
.trae/*
!.trae/documents/
.claude/
.aider*
.cursor/

# Coverage
coverage/
.nyc_output/

# Cache
.cache/
.eslintcache

# Agent workspace (developer-specific, not part of the project)
.qclaw/

# Diagnostic scripts (keep in scripts/ but never commit into repo)
scripts/scan_secrets.py

# Work artifacts (auto-generated during development)
*artifact*.md
scholarhub_*.md

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Vite cache
.vite/

# Local screenshots (committed screenshots in /screenshots are tracked separately)
screenshots/local/

# Credentials (never commit tokens/keys)
.git-credentials
*.token
*.secret
.env.keys

# Secrets / private key material (PEM, OpenSSH, PKCS#12, etc.)
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx
*.kdbx
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
id_ecdsa
id_ecdsa.pub
id_dsa
id_dsa.pub

# Cloud / package manager credentials
.aws/credentials
.aws/config
.netrc
.npmrc
.pypirc
.docker/config.json
.config/gcloud/credentials.db
